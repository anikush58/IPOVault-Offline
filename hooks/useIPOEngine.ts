import { useEffect, useRef } from 'react';
import { useSQLiteContext } from 'expo-sqlite';
import { IPOService } from '@/services/ipo/ipoService';

export function useIPOEngine() {
  // Disabled - Local Mode Only
  return;
}
