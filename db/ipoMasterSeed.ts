import { IPOMasterRecord } from '@/services/ipo/types';

/**
 * Empty Dataset - All IPO records are dynamically fetched from the Admin Backend API.
 */
export const INITIAL_IPO_MASTER_DATA: Partial<IPOMasterRecord>[] = [];
