export const AUTO_SYNC_ENABLED_KEY = 'settings_auto_sync_enabled';
export const SYNC_INTERVAL_MINUTES_KEY = 'settings_sync_interval_minutes';

export function useAutoSync() {
  // Disabled - Local SQLite Mode Only
  return;
}
